var Rule = function(start, type, onEnter, onExit){
    this.start = start;
    this.type = type;
    //this.onEnter = onEnter;
    //this.onExit = onExit;
    //should probably make a treewalker which calls methods or something
};

module.exports = Rule;